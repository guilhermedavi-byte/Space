export function KpiCard({ label, value, sample, warning }: { label: string; value: string; sample?: string; warning?: boolean }) { return <div className="rounded-2xl border border-line bg-panel p-5"><span className="text-xs text-zinc-500">{label}</span><strong className="mt-4 block text-2xl font-semibold text-white">{value}</strong>{sample && <small className={`mt-2 block ${warning ? "text-amber-300" : "text-zinc-600"}`}>{sample}</small>}</div>; }

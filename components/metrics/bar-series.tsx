export function BarSeries({ title, items }: { title: string; items: { label: string; value: number; detail?: string }[] }) { const max = Math.max(1, ...items.map((item) => item.value)); return <section className="rounded-2xl border border-line bg-panel p-6"><h2 className="font-medium text-white">{title}</h2>{items.length === 0 ? <p className="mt-8 text-sm text-zinc-600">Dados insuficientes.</p> : <div className="mt-6 space-y-3">{items.map((item) => <div key={item.label} className="grid grid-cols-[90px_1fr_55px] items-center gap-3 text-xs"><span className="truncate text-zinc-500">{item.label}</span><div className="h-2 overflow-hidden rounded bg-white/5"><div className="h-full rounded bg-violet-500" style={{ width: `${Math.max(2, item.value / max * 100)}%` }} /></div><span className="text-right text-zinc-400">{item.detail ?? item.value}</span></div>)}</div>}</section>; }

const test=require('node:test');const assert=require('node:assert/strict');const {reconcile}=require('../scripts/cancellation-production-reconcile');
test('missing Firestore export is partial ambiguity, never an empty certified base',()=>{const r=reconcile({firestoreUsers:null,subscriptions:[],pedagogicalRows:[{aluno_id:'a'},{aluno_id:'a'}]});assert.equal(r.summary.complete,false);assert.equal(r.summary.total,1);assert.equal(r.summary.AMBIGUOUS,1);assert.equal(r.summary.SAFE,0);});
test('real export reversed request is a deterministic active candidate',()=>{const r=reconcile({firestoreUsers:[{id:'a',tipo:'student',ativo:true,cancelamentosAnteriores:[{dataPedido:'2026-01-01T15:00:00Z',dataEfetivacao:'2026-01-02T15:00:00Z',desfecho:'revertido'}]}]});assert.equal(r.summary.SAFE,1);assert.equal(r.summary.complete,true);});
test('contradictory canonical state is reported without mutation',()=>{const r=reconcile({firestoreUsers:[{id:'a',tipo:'student',ativo:true}],subscriptions:[{firestore_student_id:'a',lifecycle_status:'churned'}]});assert.equal(r.summary.CONFLICT,1);assert.equal(r.summary.mutations,false);});

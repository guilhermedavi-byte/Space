(() => {
  const root = () => document.querySelector('[data-admin-sdr]');
  if (!root()) return;
  const state = { data: null, loading: false, error: '', tab: 'overview', filters: { period: 'last7', sdr: 'all', status: 'all', score: 'all', result: 'all', from: '', to: '' }, callId: '' };
  const esc = v => String(v == null ? '' : v).replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const n = v => v == null || Number.isNaN(Number(v)) ? '—' : new Intl.NumberFormat('pt-BR').format(Number(v));
  const pct = v => v == null || Number.isNaN(Number(v)) ? '—' : `${Number(v).toFixed(1).replace('.', ',')}%`;
  const money = v => new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(Number(v || 0));
  const date = v => /^\d{4}-\d{2}-\d{2}$/.test(String(v||'')) ? String(v).split('-').reverse().join('/') : '—';
  const duration = s => s == null ? '—' : `${Math.floor(Number(s)/60)}m ${Math.round(Number(s)%60)}s`;
  const api = async () => {
    const p = new URLSearchParams({ ...state.filters }); if (state.callId) p.set('callId', state.callId);
    const res = await fetchWithAuth(`/api/admin-sdr?${p}`, { method: 'GET' });
    const json = await res.json().catch(() => ({})); if (!res.ok) throw new Error(json.message || json.error || 'admin_sdr_failed'); return json;
  };
  const injectStyle = () => { if (document.getElementById('admin-sdr-style')) return; const s = document.createElement('style'); s.id='admin-sdr-style'; s.textContent = `
.asdr{width:100%;max-width:1440px;margin:0 auto;padding:28px;color:#fff}.asdr *{box-sizing:border-box}.asdr p,.asdr h1,.asdr h2,.asdr h3{margin:0}.asdr-shell{border-radius:16px;padding:30px;background:radial-gradient(circle at 18% 8%,rgba(89,144,189,.16),transparent 42%),radial-gradient(circle at 86% 0%,rgba(255,78,70,.1),transparent 40%),linear-gradient(160deg,#0b1620 0%,#132436 45%,#0b1620 100%);border:1px solid rgba(89,144,189,.13);box-shadow:0 18px 50px rgba(0,0,0,.34)}.asdr-head{display:flex;align-items:flex-end;justify-content:space-between;gap:18px;margin-bottom:18px}.asdr-kicker{color:rgba(89,144,189,.72);font-size:11px;font-weight:850;letter-spacing:.18em;text-transform:uppercase;margin-bottom:7px}.asdr-title{font-size:clamp(1.9rem,3vw,2.6rem);font-weight:900;letter-spacing:-.05em}.asdr-sub{margin-top:8px;color:rgba(255,255,255,.56);font-size:.94rem}.asdr-actions{display:flex;gap:10px;flex-wrap:wrap}.asdr-btn{appearance:none;border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.05);color:#fff;border-radius:999px;min-height:39px;padding:0 14px;font:inherit;font-weight:800;font-size:.8rem;cursor:pointer}.asdr-btn.primary{border:0;background:linear-gradient(180deg,#ff6a60,#f04a44);box-shadow:0 12px 28px rgba(255,86,79,.24)}.asdr-filters{display:grid;grid-template-columns:repeat(5,minmax(150px,1fr));gap:10px;margin:18px 0}.asdr-field{display:grid;gap:6px}.asdr-field span{font-size:10px;text-transform:uppercase;letter-spacing:.12em;color:rgba(89,144,189,.75);font-weight:850}.asdr-input{width:100%;min-height:40px;border:1px solid rgba(255,255,255,.1);border-radius:13px;background:rgba(255,255,255,.055);color:#fff;padding:0 12px;font:inherit;font-size:.82rem}.asdr-tabs{display:flex;gap:8px;flex-wrap:wrap;margin:6px 0 18px}.asdr-tab{border:1px solid rgba(255,255,255,.09);background:rgba(255,255,255,.04);color:rgba(255,255,255,.72);border-radius:999px;padding:9px 12px;font-weight:850;font-size:.78rem;cursor:pointer}.asdr-tab.is-active{background:rgba(255,106,96,.16);border-color:rgba(255,106,96,.34);color:#fff}.asdr-grid{display:grid;gap:14px}.asdr-kpis{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:12px}.asdr-card{border:1px solid rgba(89,144,189,.12);border-radius:18px;background:rgba(255,255,255,.045);box-shadow:0 12px 30px rgba(0,0,0,.22);padding:18px;overflow:hidden}.asdr-card h2,.asdr-card h3{font-size:1rem;font-weight:850;margin-bottom:12px}.asdr-kpi span{display:block;color:rgba(255,255,255,.52);font-size:.7rem;font-weight:850;letter-spacing:.08em;text-transform:uppercase}.asdr-kpi strong{display:block;margin-top:8px;font-size:1.65rem;font-weight:900;letter-spacing:-.04em}.asdr-kpi small{display:block;margin-top:5px;color:rgba(255,255,255,.45)}.asdr-two{display:grid;grid-template-columns:1.1fr .9fr;gap:14px}.asdr-bars{display:grid;gap:10px}.asdr-bar-row{display:grid;grid-template-columns:140px 1fr 72px;gap:10px;align-items:center;color:rgba(255,255,255,.78);font-size:.82rem}.asdr-track{height:12px;border-radius:99px;background:rgba(255,255,255,.06);overflow:hidden}.asdr-track i{display:block;height:100%;background:linear-gradient(90deg,#ff6a60,#7ab7ff);border-radius:inherit}.asdr-table-wrap{overflow:auto}.asdr-table{width:100%;border-collapse:collapse;min-width:980px}.asdr-table th,.asdr-table td{padding:11px 10px;border-bottom:1px solid rgba(255,255,255,.06);text-align:left;font-size:.82rem}.asdr-table th{color:rgba(255,255,255,.5);font-size:.68rem;text-transform:uppercase;letter-spacing:.1em}.asdr-badge{display:inline-flex;border-radius:999px;padding:4px 9px;background:rgba(255,255,255,.07);font-weight:850;font-size:.72rem}.asdr-badge.ok{color:#8ef0c3;background:rgba(93,202,165,.12)}.asdr-badge.warn{color:#ffd182;background:rgba(251,191,36,.12)}.asdr-badge.bad{color:#ffaaa5;background:rgba(255,106,96,.13)}.asdr-empty{display:grid;place-items:center;min-height:180px;text-align:center;color:rgba(255,255,255,.58);gap:8px}.asdr-call{display:grid;grid-template-columns:minmax(0,1fr) 340px;gap:14px}.asdr-pre{white-space:pre-wrap;color:rgba(255,255,255,.72);line-height:1.6}.asdr-audio{width:100%}.asdr-muted{color:rgba(255,255,255,.5)}.asdr-insights{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.asdr-insights .asdr-card strong{display:block;margin-bottom:8px}.asdr-footer{margin-top:14px;color:rgba(255,255,255,.44);font-size:.76rem}@media(max-width:1100px){.asdr-kpis{grid-template-columns:repeat(3,1fr)}.asdr-two,.asdr-call{grid-template-columns:1fr}.asdr-filters{grid-template-columns:repeat(2,1fr)}.asdr-insights{grid-template-columns:1fr}}@media(max-width:720px){.asdr{padding:16px}.asdr-shell{padding:20px}.asdr-head{display:grid}.asdr-kpis,.asdr-filters{grid-template-columns:1fr}}`; document.head.append(s); };
  const kpi = (label, value, sub='') => `<article class="asdr-card asdr-kpi"><span>${esc(label)}</span><strong>${esc(value)}</strong>${sub?`<small>${esc(sub)}</small>`:''}</article>`;
  const renderFilters = data => `<div class="asdr-filters">
    <label class="asdr-field"><span>Data</span><select class="asdr-input" data-asdr-filter="period">${[['today','Hoje'],['yesterday','Ontem'],['last7','Últimos 7 dias'],['this_week','Esta semana'],['last_week','Semana passada'],['last30','Últimos 30 dias'],['this_month','Este mês'],['last_month','Mês passado'],['custom','Personalizado']].map(([v,l])=>`<option value="${v}" ${state.filters.period===v?'selected':''}>${l}</option>`).join('')}</select></label>
    <label class="asdr-field"><span>SDR</span><select class="asdr-input" data-asdr-filter="sdr"><option value="all">Todos</option>${(data.sdrOptions||[]).map(s=>`<option value="${esc(s.uid)}" ${state.filters.sdr===s.uid?'selected':''}>${esc(s.name)}</option>`).join('')}</select></label>
    <label class="asdr-field"><span>Status</span><select class="asdr-input" data-asdr-filter="status">${[['all','Todas'],['connected','Conectadas'],['unanswered','Não atendidas'],['over1m','> 1 minuto'],['over2m','> 2 minutos'],['over5m','> 5 minutos'],['over10m','> 10 minutos']].map(([v,l])=>`<option value="${v}" ${state.filters.status===v?'selected':''}>${l}</option>`).join('')}</select></label>
    <label class="asdr-field"><span>Nota IA</span><select class="asdr-input" data-asdr-filter="score">${[['all','Todas'],['90','90–100'],['80','80–89'],['70','70–79'],['60','60–69'],['lt60','Abaixo de 60']].map(([v,l])=>`<option value="${v}" ${state.filters.score===v?'selected':''}>${l}</option>`).join('')}</select></label>
    <label class="asdr-field"><span>Resultado</span><select class="asdr-input" data-asdr-filter="result">${[['all','Todos'],['none','Sem resultado'],['scheduled','Agendamento'],['show','Show'],['noshow','No-show'],['sale','Venda'],['lost','Perdida']].map(([v,l])=>`<option value="${v}" ${state.filters.result===v?'selected':''}>${l}</option>`).join('')}</select></label>
  </div>`;
  const renderKpis = d => { const k=d.kpis||{}; return `<section class="asdr-kpis">${[
    kpi('Total de ligações', n(k.totalCalls)), kpi('Conectadas', n(k.connected), pct(k.totalCalls?pct(k.connected,k.totalCalls):null)), kpi('> 1 minuto', n(k.over1m), 'Aguardando duração'), kpi('> 5 minutos', n(k.over5m), 'Aguardando duração'), kpi('Tempo em chamadas', duration(k.totalDurationSeconds)), kpi('Duração média', duration(k.avgDurationSeconds)), kpi('Agendamentos', n(k.scheduled), pct(k.callToScheduleRate)), kpi('Shows', n(k.shows)), kpi('No-shows', n(k.noShows)), kpi('Show rate', pct(k.showRate)), kpi('Vendas', n(k.sales)), kpi('Receita originada', money(k.revenue)), kpi('Ticket médio', k.avgTicket==null?'—':money(k.avgTicket)), kpi('Calls analisadas IA', n(k.analyzedCalls)), kpi('Nota média', k.avgScore==null?'—':n(k.avgScore)), kpi('Aderência script', pct(k.scriptAdherence))].join('')}</section>`; };
  const renderFunnel = d => `<section class="asdr-card"><h2>Funil SDR</h2><div class="asdr-bars">${(d.funnel||[]).map((x,i,a)=>{const max=Math.max(1,Number(a[0]?.value||0));const v=x.value==null?0:Number(x.value||0);return `<div class="asdr-bar-row"><span>${esc(x.label)}</span><div class="asdr-track"><i style="width:${Math.round((v/max)*100)}%"></i></div><strong>${x.value==null?'—':n(x.value)} ${x.rateFromPrevious==null?'':`· ${pct(x.rateFromPrevious)}`}</strong></div>`}).join('')}</div></section>`;
  const renderTimeline = d => `<section class="asdr-card"><h2>Evolução</h2><div class="asdr-bars">${(d.timeline||[]).slice(-14).map(x=>{const max=Math.max(1,...(d.timeline||[]).map(r=>Number(r.calls||0)));return `<div class="asdr-bar-row"><span>${esc(date(x.dateKey))}</span><div class="asdr-track"><i style="width:${Math.round((Number(x.calls||0)/max)*100)}%"></i></div><strong>${n(x.calls)} calls · ${n(x.scheduled)} ag.</strong></div>`}).join('') || '<div class="asdr-empty">Dados insuficientes para calcular evolução.</div>'}</div></section>`;
  const renderSdrs = d => `<section class="asdr-card"><h2>Performance dos SDRs</h2><div class="asdr-table-wrap"><table class="asdr-table"><thead><tr><th>SDR</th><th>Ligações</th><th>Conectadas</th><th>>1min</th><th>>5min</th><th>Agend.</th><th>Conv.</th><th>Shows</th><th>Show rate</th><th>Vendas</th><th>Receita</th><th>Nota IA</th><th>Script</th><th>Ações</th></tr></thead><tbody>${(d.sdrs||[]).map(r=>`<tr><td><strong>${esc(r.name)}</strong><br><span class="asdr-muted">${esc(r.email)}</span></td><td>${n(r.calls)}</td><td>${n(r.connected)}</td><td>${n(r.over1m)}</td><td>${n(r.over5m)}</td><td>${n(r.scheduled)}</td><td>${pct(r.callToScheduleRate)}</td><td>${n(r.shows)}</td><td>${pct(r.showRate)}</td><td>${n(r.sales)}</td><td>${money(r.revenue)}</td><td>${r.avgScore==null?'—':n(r.avgScore)}</td><td>${pct(r.scriptAdherence)}</td><td><button class="asdr-btn" data-asdr-sdr="${esc(r.uid)}">Ver SDR</button></td></tr>`).join('') || '<tr><td colspan="14">Nenhum SDR encontrado.</td></tr>'}</tbody></table></div></section>`;
  const renderCalls = d => `<section class="asdr-card"><h2>Ligações e eventos</h2><div class="asdr-table-wrap"><table class="asdr-table"><thead><tr><th>Data</th><th>SDR</th><th>Telefone/Lead</th><th>Duração</th><th>Status</th><th>Agendamento?</th><th>Show?</th><th>Venda?</th><th>Nota IA</th><th>Aderência</th><th>Gravação</th><th>Análise</th></tr></thead><tbody>${(d.calls||[]).map(c=>`<tr><td>${date(c.dateKey)} ${esc(c.time)}</td><td>${esc(c.sdrName)}</td><td>${esc(c.phone||c.leadName||'—')}</td><td>${duration(c.durationSeconds)}</td><td><span class="asdr-badge ${c.status==='connected'?'ok':c.status==='unanswered'?'bad':'warn'}">${esc(c.status==='connected'?'Conectada':c.status==='unanswered'?'Não atendida':'Reunião')}</span></td><td>${c.outcome==='scheduled'?'Sim':'—'}</td><td>${c.outcome==='show'?'Sim':c.outcome==='noshow'?'No-show':'—'}</td><td>—</td><td>${c.score==null?'—':n(c.score)}</td><td>${pct(c.scriptAdherence)}</td><td>${c.recording?'<audio class="asdr-audio" controls src="'+esc(c.recording)+'"></audio>':'—'}</td><td><button class="asdr-btn" data-asdr-call="${esc(c.id)}">Ver análise</button></td></tr>`).join('') || '<tr><td colspan="12">Nenhuma ligação encontrada.</td></tr>'}</tbody></table></div></section>`;
  const renderInsights = d => `<section class="asdr-insights">${(d.insights||[]).map(i=>`<article class="asdr-card"><strong>${esc(i.title)}</strong><p class="asdr-muted">${esc(i.detail)}</p></article>`).join('')}</section>`;
  const renderObjections = d => `<section class="asdr-card"><h2>Objeções</h2>${(d.objections||[]).length?'<div></div>':'<div class="asdr-empty">Nenhuma objeção IA encontrada no período.</div>'}</section>`;
  const renderScorecards = d => `<section class="asdr-card"><h2>Scorecards</h2>${(d.scorecard?.criteria||[]).length?'<div></div>':'<div class="asdr-empty">Aguardando scorecards IA persistidos.</div>'}</section>`;
  const renderCoaching = d => `<section class="asdr-card"><h2>Coaching automático</h2><div class="asdr-grid">${(d.coaching||[]).map(c=>`<article class="asdr-card"><h3>${esc(c.sdrName)}</h3><p><strong>${esc(c.skill)}</strong></p><p class="asdr-muted">${esc(c.evidence)}</p><p>${esc(c.recommendation)}</p></article>`).join('') || '<div class="asdr-empty">Dados insuficientes.</div>'}</div></section>`;
  const renderLibrary = d => `<section class="asdr-card"><h2>Biblioteca de Calls</h2><div class="asdr-empty">Melhores calls, calls para revisão e favoritas aparecem aqui quando houver gravações/análises persistidas.</div></section>`;
  const renderRanking = d => `<section class="asdr-card"><h2>Ranking por métrica</h2>${renderSdrs(d)}</section>`;
  const renderCallDetail = d => { const c=d.selectedCall; if(!c) return renderCalls(d); return `<section class="asdr-call"><article class="asdr-card"><h2>Análise da ligação</h2><p class="asdr-muted">${date(c.dateKey)} ${esc(c.time)} · ${esc(c.sdrName)} · ${esc(c.callId)}</p>${c.recording?`<audio class="asdr-audio" controls src="${esc(c.recording)}"></audio>`:'<div class="asdr-empty">Gravação não disponível.</div>'}<h3>Transcrição</h3><p class="asdr-pre">${esc(c.transcript || 'Transcrição ausente.')}</p></article><aside class="asdr-card"><h3>Score</h3><p class="asdr-title">${c.score==null?'—':n(c.score)}</p><p class="asdr-muted">${esc(c.analysisText || '')}</p><button class="asdr-btn" data-asdr-tab="calls">Voltar</button></aside></section>`; };
  const tabMarkup = () => [['overview','Visão Geral'],['sdrs','SDRs'],['calls','Ligações'],['scorecards','Scorecards'],['coaching','Coaching'],['objections','Objeções'],['ranking','Ranking'],['library','Biblioteca']].map(([id,l])=>`<button class="asdr-tab ${state.tab===id?'is-active':''}" data-asdr-tab="${id}">${l}</button>`).join('');
  const body = d => state.callId ? renderCallDetail(d) : state.tab==='sdrs'?renderSdrs(d):state.tab==='calls'?renderCalls(d):state.tab==='scorecards'?renderScorecards(d):state.tab==='coaching'?renderCoaching(d):state.tab==='objections'?renderObjections(d):state.tab==='ranking'?renderRanking(d):state.tab==='library'?renderLibrary(d):`${renderKpis(d)}<div class="asdr-two">${renderFunnel(d)}${renderTimeline(d)}</div>${renderSdrs(d)}${renderInsights(d)}${renderCalls({...d,calls:(d.calls||[]).slice(0,8)})}`;
  const render = () => { injectStyle(); const el=root(); if(!el) return; const d=state.data||{}; el.innerHTML = `<div class="asdr"><div class="asdr-shell"><header class="asdr-head"><div><p class="asdr-kicker">Admin / SDR</p><h1 class="asdr-title">Painel SDR</h1><p class="asdr-sub">Performance, ligações, scorecards e coaching dos SDRs da Space.</p></div><div class="asdr-actions"><button class="asdr-btn" data-asdr-refresh>Atualizar</button><button class="asdr-btn primary" data-asdr-export>Exportar CSV</button></div></header>${renderFilters(d)}<nav class="asdr-tabs">${tabMarkup()}</nav>${state.loading?'<div class="asdr-empty">Carregando painel SDR…</div>':state.error?`<div class="asdr-empty"><strong>Não foi possível carregar SDR agora.</strong><p>${esc(state.error)}</p><button class="asdr-btn" data-asdr-refresh>Tentar novamente</button></div>`:body(d)}<p class="asdr-footer">Última atualização: ${d.generatedAt?new Date(d.generatedAt).toLocaleString('pt-BR'):'—'} · Fonte: ${esc(d.source?.operational||'')}</p></div></div>`; };
  const load = async () => { state.loading = true; state.error=''; render(); try { state.data = await api(); } catch(e) { state.error=e.message||'Erro'; } finally { state.loading=false; render(); } };
  const exportCsv = () => { const rows = state.data?.calls || []; const csv = [['data','sdr','status','resultado','duracao','nota_ia'].join(','), ...rows.map(c=>[c.dateKey,c.sdrName,c.status,c.outcome,c.durationSeconds??'',c.score??''].map(v=>`"${String(v).replace(/"/g,'""')}"`).join(','))].join('\n'); const a=document.createElement('a'); a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'})); a.download='sdr-calls.csv'; a.click(); URL.revokeObjectURL(a.href); };
  document.addEventListener('click', e => { const t=e.target.closest('[data-asdr-tab],[data-asdr-refresh],[data-asdr-call],[data-asdr-sdr],[data-asdr-export]'); if(!t) return; if(t.hasAttribute('data-asdr-refresh')) return load(); if(t.hasAttribute('data-asdr-export')) return exportCsv(); if(t.hasAttribute('data-asdr-tab')) { state.tab=t.getAttribute('data-asdr-tab'); state.callId=''; render(); } if(t.hasAttribute('data-asdr-call')) { state.callId=t.getAttribute('data-asdr-call'); state.tab='calls'; load(); } if(t.hasAttribute('data-asdr-sdr')) { state.filters.sdr=t.getAttribute('data-asdr-sdr'); state.tab='sdrs'; load(); } });
  document.addEventListener('change', e => { const t=e.target.closest('[data-asdr-filter]'); if(!t) return; state.filters[t.getAttribute('data-asdr-filter')] = t.value; state.callId=''; load(); });
  window.SpaceAdminSdr = { open: load, render };
  if (document.body.dataset.initialPanel === 'admin-sdr') load();
})();
